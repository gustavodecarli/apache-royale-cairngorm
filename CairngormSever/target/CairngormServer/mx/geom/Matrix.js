/**
 * Generated by Apache Royale Compiler from mx/geom/Matrix.as
 * mx.geom.Matrix
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('mx.geom.Matrix');
/* Royale Dependency List: XML*/

goog.require('org.apache.royale.geom.Matrix');



/**
 * @constructor
 * @extends {org.apache.royale.geom.Matrix}
 * @param {number=} a
 * @param {number=} b
 * @param {number=} c
 * @param {number=} d
 * @param {number=} tx
 * @param {number=} ty
 */
mx.geom.Matrix = function(a, b, c, d, tx, ty) {
  a = typeof a !== 'undefined' ? a : 1;
  b = typeof b !== 'undefined' ? b : 0;
  c = typeof c !== 'undefined' ? c : 0;
  d = typeof d !== 'undefined' ? d : 1;
  tx = typeof tx !== 'undefined' ? tx : 0;
  ty = typeof ty !== 'undefined' ? ty : 0;
  mx.geom.Matrix.base(this, 'constructor', a, b, c, d, tx, ty);
};
goog.inherits(mx.geom.Matrix, org.apache.royale.geom.Matrix);


/**
 * Prevent renaming of class. Needed for reflection.
 */
goog.exportSymbol('mx.geom.Matrix', mx.geom.Matrix);


/**
 * @export
 * @param {number} width
 * @param {number} height
 * @param {number=} rotation
 * @param {number=} tx
 * @param {number=} ty
 */
mx.geom.Matrix.prototype.createGradientBox = function(width, height, rotation, tx, ty) {
  rotation = typeof rotation !== 'undefined' ? rotation : 0;
  tx = typeof tx !== 'undefined' ? tx : 0;
  ty = typeof ty !== 'undefined' ? ty : 0;
};


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
mx.geom.Matrix.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'Matrix', qName: 'mx.geom.Matrix', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
mx.geom.Matrix.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    methods: function () {
      return {
        'Matrix': { type: '', declaredBy: 'mx.geom.Matrix', parameters: function () { return [ 'Number', true ,'Number', true ,'Number', true ,'Number', true ,'Number', true ,'Number', true ]; }},
        'createGradientBox': { type: 'void', declaredBy: 'mx.geom.Matrix', parameters: function () { return [ 'Number', false ,'Number', false ,'Number', true ,'Number', true ,'Number', true ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
mx.geom.Matrix.prototype.ROYALE_COMPILE_FLAGS = 26;
