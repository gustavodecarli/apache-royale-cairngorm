/**
 * Generated by Apache Royale Compiler from mx/containers/beads/ApplicationLayout.as
 * mx.containers.beads.ApplicationLayout
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('mx.containers.beads.ApplicationLayout');
/* Royale Dependency List: mx.core.Container,mx.core.IFlexDisplayObject,org.apache.royale.utils.Language,XML*/

goog.require('mx.containers.beads.BoxLayout');



/**
 *  Constructor.
 *  
 *  @langversion 3.0
 *  @playerversion Flash 9
 *  @playerversion AIR 1.1
 *  @productversion Flex 3
 * @constructor
 * @extends {mx.containers.beads.BoxLayout}
 */
mx.containers.beads.ApplicationLayout = function() {
  mx.containers.beads.ApplicationLayout.base(this, 'constructor');
};
goog.inherits(mx.containers.beads.ApplicationLayout, mx.containers.beads.BoxLayout);


/**
 * Prevent renaming of class. Needed for reflection.
 */
goog.exportSymbol('mx.containers.beads.ApplicationLayout', mx.containers.beads.ApplicationLayout);


/**
 *  @asprivate
 *  Lay out children as per Application layout rules.
 * @export
 * @override
 */
mx.containers.beads.ApplicationLayout.prototype.updateDisplayList = function(unscaledWidth, unscaledHeight) {
  var /** @type {number} */ i = 0;
  mx.containers.beads.ApplicationLayout.superClass_.updateDisplayList.apply(this, [ unscaledWidth, unscaledHeight] );
  var /** @type {mx.core.Container} */ target = mx.containers.beads.ApplicationLayout.superClass_.get__target.apply(this);
  if ((this.getHorizontalAlignValue() > 0) || (this.getVerticalAlignValue() > 0)) {
    var /** @type {number} */ paddingLeft = Number(target.getStyle("paddingLeft"));
    var /** @type {number} */ paddingTop = Number(target.getStyle("paddingTop"));
    var /** @type {number} */ oX = 0;
    var /** @type {number} */ oY = 0;
    var /** @type {number} */ n = target.numChildren;
    //var /** @type {number} */ i = 0;
    var /** @type {mx.core.IFlexDisplayObject} */ child;
    for (i = 0; i < n; i++) {
      child = org.apache.royale.utils.Language.as(target.getChildAt(i), mx.core.IFlexDisplayObject, true);
      if (child.x < paddingLeft)
        oX = Math.max(oX, paddingLeft - child.x);
      if (child.y < paddingTop)
        oY = Math.max(oY, paddingTop - child.y);
    }
    if (oX != 0 || oY != 0) {
      for (i = 0; i < n; i++) {
        child = org.apache.royale.utils.Language.as(target.getChildAt(i), mx.core.IFlexDisplayObject, true);
        child.move(child.x + oX, child.y + oY);
      }
    }
  }
};


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
mx.containers.beads.ApplicationLayout.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'ApplicationLayout', qName: 'mx.containers.beads.ApplicationLayout', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
mx.containers.beads.ApplicationLayout.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    methods: function () {
      return {
        'ApplicationLayout': { type: '', declaredBy: 'mx.containers.beads.ApplicationLayout'},
        'updateDisplayList': { type: 'void', declaredBy: 'mx.containers.beads.ApplicationLayout', parameters: function () { return [ 'Number', false ,'Number', false ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
mx.containers.beads.ApplicationLayout.prototype.ROYALE_COMPILE_FLAGS = 26;
