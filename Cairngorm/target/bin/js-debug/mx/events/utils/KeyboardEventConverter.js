/**
 * Generated by Apache Royale Compiler from mx/events/utils/KeyboardEventConverter.as
 * mx.events.utils.KeyboardEventConverter
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('mx.events.utils.KeyboardEventConverter');
/* Royale Dependency List: mx.core.Keyboard,mx.events.KeyboardEvent,org.apache.royale.events.utils.KeyConverter,XML*/




/**
 * @constructor
 */
mx.events.utils.KeyboardEventConverter = function() {
};


/**
 * Prevent renaming of class. Needed for reflection.
 */
goog.exportSymbol('mx.events.utils.KeyboardEventConverter', mx.events.utils.KeyboardEventConverter);


/**
 *  Converts JS keyboard events to Royale ones.
 *  
 *  @langversion 3.0
 *  @playerversion Flash 10.2
 *  @playerversion AIR 2.6
 *  @productversion Royale 0.8
 *  @royaleignorecoercion Event
 * @royaleignorecoercion goog.events.Event
 * We're lying to the compiler for now because it thinks it's supposed to accept a goog.events.Event.
 * We need to fix this in typedefs
 * @export
 * @param {Object} nativeEvent
 * @param {goog.events.BrowserEvent=} browserEvent
 * @return {mx.events.KeyboardEvent}
 */
mx.events.utils.KeyboardEventConverter.convert = function(nativeEvent, browserEvent) {
  browserEvent = typeof browserEvent !== 'undefined' ? browserEvent : null;
  if (nativeEvent["getModifierState"]) {
    mx.core.Keyboard.setCapsLock(!!(nativeEvent["getModifierState"]("CapsLock")));
  }
  var /** @type {string} */ type = org.apache.royale.utils.Language.string(nativeEvent["type"]);
  var /** @type {string} */ key = org.apache.royale.utils.Language.string(nativeEvent["key"]);
  if (!key)
    key = org.apache.royale.events.utils.KeyConverter.convertCharCode((nativeEvent['charCode']) >>> 0);
  var /** @type {string} */ code = org.apache.royale.utils.Language.string(nativeEvent["code"]);
  if (code == null)
    code = org.apache.royale.events.utils.KeyConverter.convertKeyCode((nativeEvent['keyCode']) >>> 0);
  if (type == "keydown")
    type = "keyDown";
  if (type == "keyup")
    type = "keyUp";
  var /** @type {mx.events.KeyboardEvent} */ newEvent = new mx.events.KeyboardEvent(type, key, code, nativeEvent["shiftKey"]);
  if (!browserEvent) {
    browserEvent = new goog.events.BrowserEvent(nativeEvent, nativeEvent["currentTarget"]);
  }
  newEvent.wrapEvent(browserEvent);
  return newEvent;
};


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
mx.events.utils.KeyboardEventConverter.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'KeyboardEventConverter', qName: 'mx.events.utils.KeyboardEventConverter', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
mx.events.utils.KeyboardEventConverter.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    methods: function () {
      return {
        '|convert': { type: 'mx.events.KeyboardEvent', declaredBy: 'mx.events.utils.KeyboardEventConverter', parameters: function () { return [ 'Object', false ,'goog.events.BrowserEvent', true ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
mx.events.utils.KeyboardEventConverter.prototype.ROYALE_COMPILE_FLAGS = 26;
